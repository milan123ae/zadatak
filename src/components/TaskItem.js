import React from 'react'
import { ToastBody } from 'react-bootstrap'

function TaskItem({ task }) {
  return (
    <div>
        []
        <div>
            <p>{ToastBody.titles}</p>
        </div>
    </div>
  )
}

export default TaskItem